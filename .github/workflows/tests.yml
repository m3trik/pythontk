name: Tests

# Pythontk targets Windows DCCs (Maya/Max/Nuke). Its test suite uses
# ctypes.windll, c:/... paths, and Windows-specific subprocess behaviour.
# Run on windows-latest so tests match the platform they were written for.

on:
  pull_request:
    branches: [main]
  workflow_call:

jobs:
  test:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov numpy Pillow
          # pymeshlab backs the optional MeshOps engine. Installed in CI on
          # purpose: dep-gated suites silently run their skip branches only,
          # so without it the real mesh-op paths would never execute anywhere.
          pip install pymeshlab
          pip install -e .

      - name: Run tests with coverage
        run: |
          pytest --tb=short -v --cov=pythontk --cov-report=xml --cov-report=term test/

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./test/coverage.xml
          fail_ci_if_error: false

  api-registry:
    # Content-hash staleness gate for the committed API_INDEX.md /
    # API_REGISTRY.md (root CLAUDE.md documents --check as the CI gate; before
    # this it was wired into no workflow). Its own job because the generator
    # lives in the m3trik repo and expects the monorepo layout - <pkg>/ and
    # m3trik/ as siblings - so both are checked out into sibling paths, the
    # same shape tentacle's parity job uses.
    #
    # AST-only: no package install, no runtime, no git history. Deterministic
    # in a fresh checkout because --check hashes CONTENT, not mtimes, and
    # normalizes away the generation date and #L line numbers (see
    # StalenessGate) - only a real public-surface delta fails this.
    name: API registry up to date
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          path: pythontk

      - uses: actions/checkout@v4
        with:
          repository: m3trik/m3trik
          ref: main
          path: m3trik

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Registry matches source
        run: python m3trik/scripts/generate_api_registry.py pythontk --check
